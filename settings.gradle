pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // for web-scraper
        maven { url 'https://jitpack.io' } // for persian calendar
        maven { url 'https://androidx.dev/snapshots/latest/artifacts/repository' } // for glance
    }
}
rootProject.name = "UT Dining Widget"
include ':app'
